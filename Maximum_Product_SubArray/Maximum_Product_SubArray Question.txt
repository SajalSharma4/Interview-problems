Given an integer array nums, find a subarray that has the largest product, and return the product.

The maximum product subarray is the product of all the integers if the array only contains positive numbers.